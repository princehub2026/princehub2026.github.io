---
import BaseLayout from "../layouts/BaseLayout.astro";
import { counties } from "../data/counties";
const PHONE = "484-944-0834";
const PHONE_TEL = PHONE.replace(/\D/g, "");
// Web3Forms relay key — submissions are forwarded to Prince@yakubulaw.com.
// Public by design (it appears in the page source); it identifies the destination
// inbox only and can be rotated at web3forms.com if it is ever abused.
const WEB3FORMS_KEY = "77a6036d-982b-4541-b6dd-07354262c6a0";
---
<BaseLayout
  title="Contact"
  description="Contact Yakubu Law for a free consultation. Available 24/7 by phone."
>
  <section class="bg-navy-950 glow-top noise pt-16 pb-12 sm:pt-20 sm:pb-16 text-center">
    <p class="text-gold-400 text-xs tracking-wide-lg uppercase">Contact</p>
    <h1 class="font-serif text-cream-50 text-4xl sm:text-5xl mt-3">Free Consultation</h1>
    <div class="glow-rule w-16 mx-auto mt-6"></div>
  </section>

  <section class="bg-cream-50 py-14 sm:py-20">
    <div class="mx-auto max-w-4xl px-6 grid sm:grid-cols-5 gap-12">
      <div class="sm:col-span-2 sm:border-r sm:border-gold-500/30 sm:pr-10">
        <a
          href={`tel:${PHONE_TEL}`}
          class="block text-center px-6 py-5 bg-navy-950 text-cream-50 mb-8 border border-gold-500/40 shadow-[0_0_28px_-6px_rgba(201,164,74,0.35)] transition-shadow hover:shadow-[0_0_34px_-4px_rgba(201,164,74,0.5)]"
         
        >
          <span class="block text-[11px] tracking-wide-lg uppercase text-gold-400 mb-1">Call 24/7</span>
          <span class="font-serif text-xl">{PHONE}</span>
        </a>
        <a
          href="https://ujsportal.pacourts.us"
          target="_blank"
          rel="noopener noreferrer"
          class="group block border border-gold-500/35 bg-navy-950 px-6 py-7 text-center transition-shadow hover:shadow-[0_0_30px_-6px_rgba(201,164,74,0.45)]"
        >
          <svg viewBox="0 0 64 64" role="img" aria-hidden="true" class="mx-auto w-[4.5rem] h-[4.5rem]" fill="none" stroke="#c9a44a" stroke-width="1.4" stroke-linecap="round">
            <rect x="7" y="13" width="50" height="44" />
            <path d="M7 26h50" />
            <path d="M20 7v11M44 7v11" />
            <path d="M17 37h7M28.5 37h7M17 47h7" />
            <circle cx="43" cy="47" r="5" stroke-width="1.6" />
          </svg>
          <p class="font-serif text-cream-50 text-lg mt-4">When is my next court date?</p>
          <p class="text-cream-100/60 text-sm mt-2 leading-relaxed">
            Look up any Pennsylvania case on the courts' public docket portal.
          </p>
          <span class="inline-block mt-4 text-[11px] tracking-wide-lg uppercase text-gold-400 border-b border-gold-500/60 pb-0.5">
            Search the docket →
          </span>
        </a>

        <div class="hairline-static my-8"></div>

        <p class="text-navy-900/50 text-xs tracking-wide-sm uppercase mb-1">Where I Practice</p>
        <p class="text-navy-900/70 text-sm leading-relaxed">
          Bucks, Chester, Delaware, Montgomery and Philadelphia counties.<br/>
          Consultations by phone, 24/7. Meetings by appointment.
        </p>
        <img
          src="/images/attorney-contact.jpg"
          alt="Attorney Prince Charles Yakubu at the courthouse"
          class="w-full aspect-[4/3] object-cover mt-8"
        />
      </div>

      <div class="sm:col-span-3">
        <form id="contact-form" action="https://api.web3forms.com/submit" method="POST" class="flex flex-col gap-5">
          <input type="hidden" name="access_key" value={WEB3FORMS_KEY} />
          <input type="hidden" name="subject" value="New consultation request — yakubulaw.com" />
          <input type="hidden" name="from_name" value="Yakubu Law website" />
          <input type="checkbox" name="botcheck" class="hidden" style="display:none" tabindex="-1" autocomplete="off" />
          <div class="grid sm:grid-cols-2 gap-5">
            <div>
              <label for="name" class="block text-[11px] tracking-wide-sm uppercase text-navy-900/60 mb-1.5">Name</label>
              <input id="name" name="name" type="text" required class="w-full border-0 border-b border-navy-900/25 bg-transparent py-3 focus:outline-none focus:border-gold-500" />
            </div>
            <div>
              <label for="phone" class="block text-[11px] tracking-wide-sm uppercase text-navy-900/60 mb-1.5">Phone</label>
              <input id="phone" name="phone" type="tel" required class="w-full border-0 border-b border-navy-900/25 bg-transparent py-3 focus:outline-none focus:border-gold-500" />
            </div>
          </div>
          <div>
            <label for="email" class="block text-[11px] tracking-wide-sm uppercase text-navy-900/60 mb-1.5">Email</label>
            <input id="email" name="email" type="email" class="w-full border-0 border-b border-navy-900/25 bg-transparent py-3 focus:outline-none focus:border-gold-500" />
          </div>
          <div class="grid sm:grid-cols-2 gap-5">
            <div>
              <label for="county" class="block text-[11px] tracking-wide-sm uppercase text-navy-900/60 mb-1.5">County</label>
              <select id="county" name="county" class="w-full border-0 border-b border-navy-900/25 bg-transparent py-3 focus:outline-none focus:border-gold-500">
                <option value="">Select a county</option>
                {counties.map((c) => <option value={c.slug}>{c.name}</option>)}
              </select>
            </div>
            <div>
              <label for="docket" class="block text-[11px] tracking-wide-sm uppercase text-navy-900/60 mb-1.5">Docket Number <span class="normal-case text-navy-900/40">(optional)</span></label>
              <input id="docket" name="docket" type="text" placeholder="If known — it's on your court paperwork" class="w-full border-0 border-b border-navy-900/25 bg-transparent py-3 placeholder:text-navy-900/35 placeholder:text-sm focus:outline-none focus:border-gold-500" />
            </div>
          </div>
          <div>
            <label for="message" class="block text-[11px] tracking-wide-sm uppercase text-navy-900/60 mb-1.5">Message</label>
            <textarea id="message" name="message" rows="5" class="w-full border-0 border-b border-navy-900/25 bg-transparent py-3 focus:outline-none focus:border-gold-500"></textarea>
          </div>
          <p class="text-navy-900/55 text-xs leading-relaxed mt-1">
            Submitting this form does not create an attorney-client relationship. Please don't
            include confidential details about your case until we've spoken and confirmed there
            is no conflict.
          </p>
          <button
            type="submit"
            id="form-submit"
            class="self-start mt-2 px-8 py-3.5 bg-navy-900 text-cream-50 text-sm tracking-wide-sm uppercase font-medium hover:bg-navy-800 transition-colors disabled:opacity-60"
           
          >
            Send
          </button>
          <p id="form-ok" class="hidden text-sm text-navy-900/80 mt-2">
            Thank you — your message has been sent. If this is urgent, call
            <a href={`tel:${PHONE_TEL}`} class="underline">{PHONE}</a> rather than waiting on email.
          </p>
          <p id="form-err" class="hidden text-sm text-navy-900/80 mt-2">
            That didn't send. Please call <a href={`tel:${PHONE_TEL}`} class="underline">{PHONE}</a> instead.
          </p>
        </form>
      </div>
    </div>
  </section>

  <script>
    const form = document.getElementById("contact-form") as HTMLFormElement | null;
    const ok = document.getElementById("form-ok");
    const err = document.getElementById("form-err");
    const btn = document.getElementById("form-submit") as HTMLButtonElement | null;

    form?.addEventListener("submit", async (e) => {
      e.preventDefault();
      ok?.classList.add("hidden");
      err?.classList.add("hidden");
      if (btn) { btn.disabled = true; btn.textContent = "Sending…"; }
      try {
        const res = await fetch(form.action, { method: "POST", body: new FormData(form) });
        if (!res.ok) throw new Error("bad status");
        form.reset();
        ok?.classList.remove("hidden");
      } catch {
        err?.classList.remove("hidden");
      } finally {
        if (btn) { btn.disabled = false; btn.textContent = "Send"; }
      }
    });
  </script>
</BaseLayout>
